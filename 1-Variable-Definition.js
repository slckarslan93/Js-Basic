const value = "ABCDEF"; //!const ile atanan değişkenlere daha sonra bir değişken ataması yapılamaz.

var value2 = "ABCDEF"; //!var ile tanımlana değişekene başka değerler atayabiliriz.

let value3 = "ABCDEF"; //!Var ile let farkı var function scope özelliği taşırken, let block scope özelliği taşımaktadır. Yani let ile oluşturulan bir değişken sadece oluşturulduğu yerdeki süslü parantezler içerisinde erişilebilirdir ve dışarısında kullanılamaz.

console.log(value);
